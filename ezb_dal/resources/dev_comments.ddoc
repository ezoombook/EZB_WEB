{
    "views" : {
        "by_ezb" : {
            "map" :
                "function (doc, meta) {
                  if(doc.commentId && doc.ezbId){
                    emit(doc.ezbId, null);
                  }
                }"
        }
    }
}
