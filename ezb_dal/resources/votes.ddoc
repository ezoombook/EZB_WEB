{
    "views" : {
        "by_votecount" : {
            "map" :
                "function(doc,meta){
                    if(meta.id.indexOf('votecount::') != -1){
                        emit(meta.id.split('::')[1], doc)
                    }
                }",
            "reduce" : "_sum"
        }
    }
}
