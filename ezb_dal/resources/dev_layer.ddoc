{
    "views" : {
        "by_ezb_level" : {
            "map" :
                "function (doc, meta) {
                   if(doc.ezoomlayer_level && doc.ezoombook_id){
                     emit([doc.ezoombook_id, doc.ezoomlayer_level], null)
                   }
                 }"
        }
    }
}